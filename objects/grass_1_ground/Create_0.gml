event_inherited();

properties_layer_base_depth = properties_layer_base_depth + (0 * global.LAYERS.ground.depth_period)

self.properties_bg_sprite = bg_grass_terrain_1_ground; // Przypisz sprite tła

after_create_event();